const Section = () => {
    return (
        <section className="container bg-blue-800 py-9 max-w-full flex justify-center">
            <h2 className="font-bold text-xl text-black">여기는 헤더입니다</h2>
        </section>
    );
};

export default Section;